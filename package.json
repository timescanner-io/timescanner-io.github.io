{
  "name": "timescanner-website",
  "version": "1.0.0",
  "description": "Site web de TimeScanner",
  "scripts": {
    "build:css": "postcss assets/css/main.css -o assets/css/build.css",
    "watch:css": "postcss assets/css/main.css -o assets/css/build.css --watch",
    "build": "npm run build:css && jekyll build",
    "serve": "npm run watch:css & jekyll serve"
  },
  "dependencies": {
    "lucide": "^0.485.0"
  },
  "devDependencies": {
    "postcss": "^8.4.35",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.1",
    "autoprefixer": "^10.4.17",
    "@tailwindcss/typography": "^0.5.10"
  }
}
